// Prisma schema file
// https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: ここにモデルを定義してください
// 例:
// model User {
//   id    Int     @id @default(autoincrement())
//   email String  @unique
//   name  String?
// }
