# GitHub Actions 実践例

> **対象者**: 実際のプロジェクトで GitHub Actions を導入したい開発者  
> **所要時間**: 約 30 分  
> **前提知識**: `01_基本情報.md` ～ `06_シークレット環境変数管理.md`

---

## 📚 目次

- [GitHub Actions 実践例](#github-actions-実践例)
  - [📚 目次](#-目次)
  - [1. Next.js プロジェクトの CI/CD](#1-nextjs-プロジェクトの-cicd)
    - [1.1 シンプルなテスト・ビルド](#11-シンプルなテスト・ビルド)
    - [1.2 複数バージョンテスト](#12-複数バージョンテスト)
  - [2. Python プロジェクトの CI/CD](#2-python-プロジェクトの-cicd)
    - [2.1 FastAPI の自動テスト](#21-fastapi-の自動テスト)
    - [2.2 Django の本番デプロイ](#22-django-の本番デプロイ)
  - [3. Docker ビルド・デプロイ](#3-docker-ビルドデプロイ)
    - [3.1 Docker イメージの自動ビルド](#31-docker-イメージの自動ビルド)
    - [3.2 Docker Compose 環境でのテスト](#32-docker-compose-環境でのテスト)
  - [4. 環境別デプロイ](#4-環境別デプロイ)
    - [4.1 staging・production への段階的デプロイ](#41-stagingproduction-への段階的デプロイ)
    - [4.2 AWS EC2 へのデプロイ](#42-aws-ec2-へのデプロイ)
    - [4.3 Vercel へのデプロイ](#43-vercel-へのデプロイ)
  - [5. リリースプロセスの自動化](#5-リリースプロセスの自動化)
    - [5.1 GitHub Release 自動作成](#51-github-release-自動作成)
    - [5.2 npm パッケージの自動公開](#52-npm-パッケージの自動公開)
  - [6. クロスプラットフォーム対応](#6-クロスプラットフォーム対応)
    - [6.1 Windows・macOS・Linux でのテスト](#61-windowsmacos・linux-でのテスト)
  - [7. キャッシュで高速化](#7-キャッシュで高速化)
    - [7.1 npm キャッシュ](#71-npm-キャッシュ)
    - [7.2 Docker レイヤーキャッシュ](#72-docker-レイヤーキャッシュ)
    - [7.3 Python pip キャッシュ](#73-python-pip-キャッシュ)
  - [8. 通知・監視](#8-通知・監視)
    - [8.1 Slack 通知](#81-slack-通知)
    - [8.2 成功・失敗時の通知](#82-成功・失敗時の通知)
  - [9. データベース連携](#9-データベース連携)
    - [9.1 PostgreSQL でのテスト](#91-postgresql-でのテスト)
    - [9.2 Prisma マイグレーション](#92-prisma-マイグレーション)
  - [📌 次のステップ](#-次のステップ)

---

## 1. Next.js プロジェクトの CI/CD

### 1.1 シンプルなテスト・ビルド

`.github/workflows/nextjs-ci.yml`:

```yaml
name: Next.js CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # コード取得
      - uses: actions/checkout@v4
      
      # Node.js セットアップ
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      # 依存関係インストール
      - name: Install dependencies
        run: npm ci
      
      # Lint チェック
      - name: Run ESLint
        run: npm run lint
      
      # 型チェック
      - name: Type check
        run: npm run type-check
      
      # テスト実行
      - name: Run tests
        run: npm test
      
      # ビルド
      - name: Build
        run: npm run build
      
      # ビルド成果物を保存
      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        if: success()
        with:
          name: nextjs-build
          path: .next/
          retention-days: 1
```

### 1.2 複数バージョンテスト

`.github/workflows/nextjs-matrix.yml`:

```yaml
name: Next.js Matrix Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16, 18, 20]
        next-version: [12, 13, 14]
      fail-fast: false  # すべてのバージョン組合せを実行
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: Install Next.js version
        run: npm install next@${{ matrix.next-version }}
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Build
        run: npm run build
```

---

## 2. Python プロジェクトの CI/CD

### 2.1 FastAPI の自動テスト

`.github/workflows/fastapi-test.yml`:

```yaml
name: FastAPI Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov black flake8
      
      - name: Format check with Black
        run: black --check .
      
      - name: Lint with Flake8
        run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
      
      - name: Run tests
        run: |
          pytest tests/ -v --cov=app --cov-report=xml
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-python-${{ matrix.python-version }}
```

### 2.2 Django の本番デプロイ

`.github/workflows/django-deploy.yml`:

```yaml
name: Django Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        required: true
        options:
          - staging
          - production

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run migrations
        run: python manage.py migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
      
      - name: Run tests
        run: python manage.py test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
  
  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.environment || 'staging' }}
      url: https://${{ inputs.environment || 'staging' }}.example.com
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: 22
          script: |
            cd /app
            git pull origin main
            pip install -r requirements.txt
            python manage.py migrate
            python manage.py collectstatic --noinput
            systemctl restart django
```

---

## 3. Docker ビルド・デプロイ

### 3.1 Docker イメージの自動ビルド

`.github/workflows/docker-build.yml`:

```yaml
name: Build and Push Docker Image

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ${{ secrets.DOCKER_USERNAME }}/my-app
            ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
      
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/my-app:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/my-app:buildcache,mode=max
```

### 3.2 Docker Compose 環境でのテスト

`.github/workflows/docker-compose-test.yml`:

```yaml
name: Test with Docker Compose

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Start services with Docker Compose
        run: docker-compose up -d
      
      - name: Wait for services
        run: |
          docker-compose exec -T api npm run wait-for-db
      
      - name: Run tests
        run: docker-compose exec -T api npm test
      
      - name: Check coverage
        run: docker-compose exec -T api npm run coverage
      
      - name: Stop services
        if: always()
        run: docker-compose down
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
```

---

## 4. 環境別デプロイ

### 4.1 staging・production への段階的デプロイ

`.github/workflows/multi-env-deploy.yml`:

```yaml
name: Multi-Environment Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
  
  deploy-staging:
    needs: test
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to staging
        run: |
          echo "Deploying to staging..."
          npm run deploy:staging
        env:
          DEPLOY_KEY: ${{ secrets.STAGING_DEPLOY_KEY }}
          API_URL: ${{ secrets.STAGING_API_URL }}
  
  deploy-production:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://example.com
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to production
        run: |
          echo "Deploying to production..."
          npm run deploy:prod
        env:
          DEPLOY_KEY: ${{ secrets.PROD_DEPLOY_KEY }}
          API_URL: ${{ secrets.PROD_API_URL }}
      
      - name: Notify deployment
        uses: slackapi/slack-github-action@v1
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK }}
          payload: |
            {
              "text": "✅ Production deployment completed",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Production Deployed* 🚀\n${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}"
                  }
                }
              ]
            }
```

### 4.2 AWS EC2 へのデプロイ

`.github/workflows/deploy-aws.yml`:

```yaml
name: Deploy to AWS EC2

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Build
        run: |
          npm ci
          npm run build
      
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            cd /var/www/app
            git pull origin main
            npm ci
            npm run build
            npm run migrate
            systemctl restart app
```

### 4.3 Vercel へのデプロイ

`.github/workflows/vercel-deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: vercel/action@main
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          github-comment: true  # PR にコメント投稿
```

---

## 5. リリースプロセスの自動化

### 5.1 GitHub Release 自動作成

`.github/workflows/release.yml`:

```yaml
name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Build
        run: |
          npm ci
          npm run build
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**/*
          body_path: CHANGELOG.md
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

### 5.2 npm パッケージの自動公開

`.github/workflows/publish-npm.yml`:

```yaml
name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Publish
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

---

## 6. クロスプラットフォーム対応

### 6.1 Windows・macOS・Linux でのテスト

`.github/workflows/cross-platform-test.yml`:

```yaml
name: Cross-Platform Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [18, 20]
      fail-fast: false
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: ${{ matrix.os }}
```

---

## 7. キャッシュで高速化

### 7.1 npm キャッシュ

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'  # 自動キャッシュ
      
      # または手動キャッシュ
      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
      
      - run: npm ci
```

### 7.2 Docker レイヤーキャッシュ

```yaml
- uses: docker/build-push-action@v5
  with:
    context: .
    push: true
    # キャッシュから復元
    cache-from: type=registry,ref=myrepo/myimage:buildcache
    # キャッシュに保存
    cache-to: type=registry,ref=myrepo/myimage:buildcache,mode=max
```

### 7.3 Python pip キャッシュ

```yaml
- uses: actions/setup-python@v4
  with:
    python-version: '3.11'
    cache: 'pip'

- run: pip install -r requirements.txt
```

---

## 8. 通知・監視

### 8.1 Slack 通知

```yaml
- name: Notify on success
  uses: slackapi/slack-github-action@v1
  if: success()
  with:
    webhook-url: ${{ secrets.SLACK_WEBHOOK }}
    payload: |
      {
        "text": "✅ Build Successful",
        "blocks": [{
          "type": "section",
          "text": {
            "type": "mrkdwn",
            "text": "*Build passed* on ${{ github.ref }}"
          }
        }]
      }

- name: Notify on failure
  uses: slackapi/slack-github-action@v1
  if: failure()
  with:
    webhook-url: ${{ secrets.SLACK_WEBHOOK }}
    payload: |
      {
        "text": "❌ Build Failed",
        "color": "danger"
      }
```

### 8.2 成功・失敗時の通知

```yaml
- uses: actions/github-script@v7
  if: failure()
  with:
    script: |
      github.rest.issues.createComment({
        issue_number: context.issue.number,
        owner: context.repo.owner,
        repo: context.repo.repo,
        body: '❌ Tests failed. Please check the logs.'
      })
```

---

## 9. データベース連携

### 9.1 PostgreSQL でのテスト

```yaml
jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - run: npm ci
      
      - name: Run migrations
        run: npm run migrate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
      
      - name: Run tests
        run: npm test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
```

### 9.2 Prisma マイグレーション

```yaml
- name: Run Prisma migrations
  run: npx prisma migrate deploy
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}

- name: Generate Prisma client
  run: npx prisma generate

- name: Run tests
  run: npm test
```

---

## 📌 次のステップ

次のドキュメント `08_デバッグとトラブルシューティング.md` では、以下を詳しく学びます：

1. **ワークフローのデバッグ方法**
2. **よくあるエラーと解決策**
3. **ログの見方・分析方法**

---

## 💡 実践例クイックリファレンス

```yaml
# Next.js プロジェクト
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - run: npm ci && npm test && npm run build

# Python プロジェクト
- uses: actions/setup-python@v4
  with:
    python-version: '3.11'
    cache: 'pip'
- run: pip install -r requirements.txt && pytest

# Docker
- uses: docker/build-push-action@v5
  with:
    push: true
    tags: myrepo/myimage:latest
    cache-from: type=registry,ref=myrepo/myimage:buildcache
    cache-to: type=registry,ref=myrepo/myimage:buildcache,mode=max
```

---

## 📖 参考リンク

- [GitHub Actions ベストプラクティス](https://docs.github.com/en/actions/guides)
- [Docker Build Push Action](https://github.com/docker/build-push-action)
- [Vercel Action](https://github.com/vercel/action)
- [SSH Action](https://github.com/appleboy/ssh-action)
